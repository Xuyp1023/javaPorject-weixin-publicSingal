package com.publicSignal.service;

import com.publicSignal.mapper.PublicSignalEffectiveLog;

public interface PublicSignalEffectiveLogService {
    
    public PublicSignalEffectiveLog saveAddLog(PublicSignalEffectiveLog effectiveLog);

}
